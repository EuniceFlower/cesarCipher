const cipher = {
  encode: (offset,string)=>{
    return encodeString(offset,string);
  },
  cipher:(offset,string)=>{
    return cipherString();
  }
};

function encodeString(offset,string){

  return message;
}

function cipherString(){

    return message;
}

export default cipher;
